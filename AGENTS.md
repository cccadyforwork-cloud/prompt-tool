# 项目固定规则

## 产品图片下载命名

- 保存或下载一组产品图片时，文件夹和压缩包只使用当前产品的中文产品名命名，例如 `产品名`、`产品名.zip`；不得使用英文产品名。
- 组内图片只使用“中文产品名 + 连续阿拉伯数字 + 原扩展名”，例如 `产品名1.png`、`产品名2.png`；不得混入英文产品名。
- 不得追加日期、图组、选中、历史记录、生成轮次、图片用途、图位名称、连字符、下划线或其他标注。
- 网页的一键保存和生成历史下载必须使用同一规则；此规则对刷新页面和所有新标签页生效。
- 中文产品名按“已提取的中文产品名 → 1688 中文标题或源文件名”依次确定；下载命名不得使用内置类目映射。仍无法确定时统一使用 `产品套图`，不得回退为英文名称。
- 除非用户明确要求，不要改名已在此前下载完成的文件。
